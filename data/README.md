# Data

| File | Source | Size | In git |
|------|--------|------|--------|
| `aime_train.jsonl` | bundled | ~150KB | yes |
| `deepscaler_preview.jsonl` | [agentica-org/DeepScaleR-Preview-Dataset](https://huggingface.co/datasets/agentica-org/DeepScaleR-Preview-Dataset) | ~21MB | no (download) |

Download DeepScaler preview (uses HF mirror):

```bash
export HF_ENDPOINT="${HF_ENDPOINT:-https://hf-mirror.com}"
python - <<'PY'
import os
os.environ.setdefault("HF_ENDPOINT", "https://hf-mirror.com")
from datasets import load_dataset
import json
from pathlib import Path
out = Path("reasoning_branch_dataset/data/deepscaler_preview.jsonl")
out.parent.mkdir(parents=True, exist_ok=True)
ds = load_dataset("agentica-org/DeepScaleR-Preview-Dataset", split="train")
with out.open("w") as f:
    for row in ds:
        f.write(json.dumps(dict(row), ensure_ascii=False) + "\n")
print("wrote", out)
PY
```

Experiment rollouts read problems from `outputs/action_study_pilot_v2/problems.jsonl` (generated by the v2 pipeline).
