# Archive — 早期实验代码(非当前主线)

本目录存放 V2–V4.0 / SD①–④ 验证侧探索的全部代码。这些方向已在
[`outputs/reports/`](../outputs/reports/) 中被证伪,**不再维护**。

当前主线代码在 [`../eagle3/`](../eagle3/)。

| 子目录 | 内容 |
|---|---|
| `action_study/` | V3.5–V4.0 step-level 实验 + `sd_audit/` 验证侧审计 |
| `scripts/` | 旧 shell 入口(V3.x pipeline、GPT oracle 等) |
| `pkg/` | 旧根目录 Python 模块(pipeline、grading、model_utils 等) |
| `tests/` | 旧单元测试 |
| `docs/` | 旧设计文档与 ROADMAP |

如需复现历史实验,报告中的路径需将 `action_study/` 替换为 `archive/action_study/`。
