# EAGLE-3 benchmark + training pipeline
vllm>=0.8.0
torch>=2.5.0
transformers>=4.45.0
datasets>=3.0.0
safetensors>=0.4.0
numpy>=1.26.0

# SpecForge training (install separately in SpecForge repo)
# deepspeed, specforge
